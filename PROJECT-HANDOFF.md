# Barry — Project Handoff

## Current state

This repository contains the Barry Xcode project in `Barry/`. Generated Xcode build output is intentionally excluded from Git.

## Next exact step

1. Open `Barry/Barry.xcodeproj` in Xcode.
2. Confirm the app builds on the current development machine.
3. Update this file with the current feature, unresolved issues, and next task before switching computers or AI assistants.

## Cross-device setup

Clone the private repository, open the Xcode project, select the appropriate signing team locally, and rebuild generated files. Do not commit signing certificates, passwords, API keys, or local Claude permission settings.

